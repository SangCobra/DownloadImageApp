package com.storm.data.repository

import com.storm.domain.model.DogImage
import com.storm.domain.repository.DogRepository

class DogRepositoryImpl: DogRepository {
    override fun getListDogs(): List<DogImage> {

    }
}